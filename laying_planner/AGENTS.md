## Agent skills

### Issue tracker

Until the user explicitly enables GitHub tracking, use `project.md/PLAN.md` or 'project.md/PLAN1.md' as the sole task and progress source; after enablement, follow `docs/agents/issue-tracker.md`.

### Triage labels

Use the five default canonical triage labels. See `docs/agents/triage-labels.md`.

### Domain docs

Use the single-context domain documentation layout. See `docs/agents/domain.md`.

### 检查

每次完成一个ticket要核实验证是否符合设计文档
