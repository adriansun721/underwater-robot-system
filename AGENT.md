# AGENTS.md

## 项目结构

本目录是水下双机器人系统的总项目目录。

各子目录职责独立：

- `underwater-robot/`：铺缆机器人规划算法
- `scout_planner/`：前导机器人规划算法
- `localization/`：双机器人定位算法
- `perception_mapping/`：前导机器人感知与建图
- `simulation/`：DAVE / Gazebo / ArduSub 仿真环境
- `interfaces/`：跨模块公共接口与数据契约
- `docs/`：系统级设计与集成文档

## 开发原则

1. 每个任务默认只修改其所属子模块，开发的时候用户会告诉你具体的模块。
2. 可以读取其他子模块的代码、设计文档和接口定义作为参考。
3. 不得因为“方便实现”而擅自修改其他模块。
4. 如发现其他模块存在问题，应先报告，不要顺手修复。
5. 跨模块修改必须明确说明：
   - 为什么必须修改；
   - 涉及哪些模块；
   - 是否改变公共接口或行为；
   - 对现有测试和兼容性的影响。
6. 公共接口应优先通过 `interfaces/` 统一定义，不允许各模块自行复制一套不兼容的数据结构。
7. 系统级约定优先记录在 `docs/`，不要把系统架构规则散落到单个算法仓库。
8. 各子模块保持独立 Git 和独立构建边界，不要在总目录下混合提交或统一构建。
9. 不得修改与当前任务无关的未提交工作。
10. 在修改任何模块前，先阅读该模块内部自己的 `AGENTS.md`、设计文档和 PLAN；子模块规则比本文件更具体时，以子模块规则为准。

## 核心原则

跨模块：可以参考，禁止乱改。

当前任务属于哪个模块，就优先只在那个模块内完成。
