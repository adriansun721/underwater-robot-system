# 水下双机器人系统

本上下文定义双机器人协同铺缆系统中跨规划、执行与控制边界使用的统一领域语言。

## Language

**联合执行剖面（Execution Profile）**:
由铺缆规划器唯一拥有并发布的不可变计划产物，统一描述履带运动、对地速度、放缆速度与加速度及张力设定值，并与计划版本和执行租约绑定。
_Avoid_: 控制目标草稿、可变控制参数、mission_manager 二次规划结果

**执行适配层（Execution Adapter）**:
消费获批联合执行剖面并负责状态管理、剖面采样、协议转换及更保守安全限幅的边界层；它不得重新计算、放宽或静默修改剖面目标。
_Avoid_: 二次规划器、剖面优化器

**执行租约（Execution Lease）**:
允许一个物理执行授权域内的特定不可变计划前缀在限定依赖和时间窗内执行的短期授权；它不能跨域转移或脱离原计划续用。
_Avoid_: 永久授权、安全结果缓存

**授权执行包（Authorized Execution Bundle）**:
一个物理执行授权域内发布执行权的唯一原子载体，将不可变计划、获批前缀、执行租约、依赖、配置及内容身份绑定为不可拆分的整体；主机铺缆域与前导运动域使用不同强类型载体。
_Avoid_: 分离缓存后拼装的计划与租约、仅凭 PlanningResult 执行、仅凭 ExecutionLease 执行

**执行授权权威（Execution Authority）**:
在一个物理执行授权域内唯一签发原子授权和规范软件撤租的逻辑权威；每个物理执行授权域恰有一个此类权威，彼此不能代理或共享授权历史。
_Avoid_: 全系统单一授权者、跨域代签、跨域共享水位

**物理执行授权域（Physical Execution Authority Domain）**:
由同一组物理执行器、独立本地安全时钟和硬停止路径形成的授权边界；本系统分别具有主机铺缆域与前导运动域。
_Avoid_: 机器人任务域、通信域、跨设备全局授权域

**主机铺缆授权域（Main Laying Authority Domain）**:
覆盖主机器人履带、放缆和张力动作的物理执行授权域。
_Avoid_: 前导授权域、全系统授权域

**前导运动授权域（Scout Motion Authority Domain）**:
覆盖前导机器人 FCU 运动目标的物理执行授权域。
_Avoid_: 主机铺缆授权域、补探任务授权

**硬急停通道（Hard Emergency Stop Channel）**:
可由操作员、安全监督器或 STM32F4 本地保护直接触发的独立安全路径，不等待执行授权权威。触发即使当前授权失效，解除后也必须取得新的授权执行包。
_Avoid_: 软件撤租替代急停、解除急停后恢复旧租约

**时钟域（Clock Domain）**:
由一次设备启动会话定义的单调时间比较范围；只有同一时钟域内的时间点才能用于安全期限、租约和顺序判断。时钟域变化或设备重启使既有执行授权失效。
_Avoid_: 跨设备直接比较单调时间、跨重启续租

**同步观测时间（Synchronized Observation Time）**:
带同步状态和不确定度的跨设备时间，仅用于观测对齐与审计，不参与本地安全 deadline 或 watchdog。
_Avoid_: 用 UTC 驱动本地安全超时、把同步时间当作单调时间

**任务世界帧（mission_enu）**:
跨模块世界与地图数据使用的右手 ENU 规范坐标帧，轴向为东、北、上。NED 数据必须在硬件适配边界转换后才能进入公共契约。
_Avoid_: world、任务定义坐标系、未标明轴向的 map frame

**机器人本体帧（base_link）**:
跨模块机器人本体数据使用的右手 FLU 规范坐标帧，轴向为前、左、上。
_Avoid_: body、FRD、未标明轴向的机器人坐标系

**ENU 偏航角（ENU Yaw）**:
绕任务世界帧正 Z 轴逆时针为正的弧度角，公共发布值规范化至 `[-pi, pi)`。
_Avoid_: heading、航向角、未声明零轴和正方向的 yaw

**NUC 公共消息契约（NUC Public Message Contract）**:
由 Protocol Buffers v3 schema 唯一定义的 NUC 级跨进程及双机器人消息语义；ROS 1、ROS 2 和规划器 C++ 类型只能适配该契约，不能扩展公共语义。
_Avoid_: ROS 消息即权威契约、C++ ABI 即公共协议

**主机 CAN 契约（Main Robot CAN Contract）**:
由 DBC 或等价固定 CAN 协议定义唯一描述的 NUC 与 STM32F4 硬实时通信语义，并与 NUC 公共消息契约共享单位、枚举、错误码和范围定义。
_Avoid_: 直接复用 Protobuf 布局、手工复制的不一致 CAN 结构体

**安全关键字段（Safety-Critical Field）**:
其缺失或未知取值可能改变授权、运动、停车、急停或故障处置的契约字段。缺失字段和未知安全关键枚举必须拒绝，不能使用默认值或静默兼容。
_Avoid_: 缺失即默认、未知枚举继续执行

**时序配置（Timing Profile）**:
统一约束执行相关发布频率、时延、新鲜度、确认、租约和 watchdog 的版本化运行配置，并定义跨层超时顺序不变量。生产运行不得缺省该配置，规划器内部时间默认值也不得直接成为控制执行契约。
_Avoid_: schema 内置策略默认值、模块私有超时、未标定生产时序

**联调时序配置（integration/v1）**:
用于非生产系统联调的首个时序配置，提供可执行的频率和超时基线，但不构成生产标定或验收证据。
_Avoid_: production 默认配置、生产能力证明

**生产者会话（Producer Session）**:
由生产者一次进程或设备生命周期定义的消息序号范围；公共消息序号只在生产者、会话和消息流的组合内严格递增，重启后必须创建新会话且旧会话不得复用。
_Avoid_: 跨重启续用序号、所有消息共享全局序号

**业务版本（Business Version）**:
表示计划、执行剖面、租约或配置的演进顺序，由对应业务权威维护；它不承担消息投递排序或内容完整性证明。
_Avoid_: 用消息 sequence 代替计划版本、用版本号代替内容摘要

**内容身份（Content Identity）**:
由规范化内容的 SHA-256 摘要表示的不可变产物身份，用于证明接收内容与获批内容完全一致。
_Avoid_: 仅凭版本号判定内容相同、非规范化序列化哈希

**正交状态域（Orthogonal State Domain）**:
规划、主机铺缆授权、前导运动授权、主机器人模式、铺缆阶段、通信、急停和前导任务分别维护的独立状态机。状态转换只改变所属状态域，跨域影响必须通过显式事件触发。
_Avoid_: 组合式全局状态枚举、跨域隐式状态修改

**执行许可（can_execute）**:
在一个物理执行授权域内，由该域授权包及租约、运行阶段、通信策略和本地安全状态共同计算的派生谓词；主机铺缆与前导运动许可彼此独立且均不可直接写入。
_Avoid_: 可写 can_execute 标志、规划成功即允许运动、通信恢复即恢复授权

**通信通道（Communication Channel）**:
具有独立身份、通信状态、时序配置和失联策略的一条具体链路。链路失效只触发其所属故障域的降级行为，不形成全系统统一通信状态。
_Avoid_: 全局 COMM_DEGRADED、跨故障域联动停机

**失联策略（Loss Policy）**:
针对特定通信通道在降级或失联时允许完成的授权动作、停车方式和恢复前置条件。
_Avoid_: 所有链路共用一套失联动作、通信恢复即自动继续

**结果码（Outcome Code）**:
一次命令、规划或状态转换的终止结果，不表示故障仍处于活动或锁存状态。
_Avoid_: FaultCode、活动告警

**故障码（Fault Code）**:
表示活动或锁存系统故障的稳定数值身份，其严重度、安全效果、锁存和清除规则由本地同版本代码注册表确定。
_Avoid_: 自由文本错误、发送方临时严重度、CAN 私有故障语义

**诊断码（Diagnostic Code）**:
表示观察、告警、数据拒绝或次级原因的稳定数值身份，本身不授予执行权。
_Avoid_: 用诊断消息授予执行、用自由文本驱动控制

**配置快照（Configuration Snapshot）**:
经过完整约束校验且具有 ID、版本和内容身份的不可变安全关键参数集合，作为授权执行包的显式依赖。运行中不得原地修改。
_Avoid_: 可变参数字典、缺失字段沿用旧值、模块私有安全参数

**配置激活（Profile Activation）**:
通过 prepare/activate 两阶段协议使通信两端绑定同一配置快照和新会话的过程。激活只证明配置一致性，不产生执行授权。
_Avoid_: 配置激活即允许运动、旧会话沿用新配置

**规范发布者（Canonical Publisher）**:
对某类安全相关消息或状态域拥有唯一发布权的逻辑角色。其他模块只能向该角色提交事实、事件或请求，不能发布竞争状态。
_Avoid_: 多发布者状态流、订阅者自行仲裁

**急停状态所有者（Emergency Stop State Owner）**:
唯一维护规范急停状态及事件序列的逻辑角色。硬急停允许多个物理源直接触发安全动作，但这些源不各自形成竞争的急停状态机。
_Avoid_: 单一物理急停源、多个规范急停状态发布者

**应用层交付语义（Application Delivery Semantics）**:
由公共契约按消息类别显式规定的确认、重试、排序和幂等行为，不从 ROS、TCP、UDP 或 CAN 的传输特性推断。
_Avoid_: TCP 可靠即命令可靠、ROS topic 即状态一致

**最新值遥测（Latest-Value Telemetry）**:
以当前最新有效观测为目标、不得在恢复后补发积压旧值的连续反馈流。
_Avoid_: 遥测事件日志、旧控制反馈回放队列

**契约清单（Contract Manifest）**:
在建链、配置激活和武装前用于判定兼容性的版本化清单，绑定 Protobuf、代码注册表、CAN 协议、时序配置及受支持特性及其内容身份。
_Avoid_: 仅比较应用版本、能反序列化即兼容

**安全语义兼容（Safety-Semantic Compatibility）**:
两个实现对安全关键字段、枚举、状态转换、授权、撤租、超时及故障效果具有经测试证明的一致理解。结构可解析不等于安全语义兼容。
_Avoid_: Protobuf 可解析即安全兼容、未经证明的混合版本执行

**安全契约一致性套件（Safety Contract Conformance Suite）**:
由 `interfaces/` 拥有并作为合并与发布强制门禁的独立测试套件，验证版本、单位、时间域、状态机、授权原子性、幂等、CAN、故障、降级、急停及端到端失败关闭语义。
_Avoid_: schema 编译即契约通过、仅模块内部单元测试

**受控停车（Controlled Stop）**:
终止当前任务授权后，由独立认证的停车授权和停车剖面在有效制动证据内协调降低履带、放缆与张力目标的停车动作。
_Avoid_: 继续使用任务 Bundle、发送零指令

**保护停车（Protective Stop）**:
由本地预认证安全逻辑针对反馈、链路或控制异常执行的快速协调停车，不等待规划、网络确认或新任务授权。
_Avoid_: 远程规划停车、自动恢复旧任务

**紧急停车（Emergency Stop）**:
由独立硬通道触发并锁存、完全由本地预认证安全逻辑执行的最高等级停车。每个可武装的管缆配置必须绑定适用的已认证急停策略。
_Avoid_: 全部输出简单清零、缺少策略仍允许武装

**急停策略（Emergency Stop Policy）**:
针对具体机械、电控和管缆配置认证的履带制动、受限释缆、张力保持、机构锁止及高功率切断组合。
_Avoid_: 通用急停默认动作、运行时临时拼装策略

**条件已消失（Cleared Condition）**:
表示故障触发条件当前不再成立的事实，不自动清除故障、解除急停、重新武装或恢复执行。
_Avoid_: 故障已复位、可以自动继续

**安全恢复链（Safety Recovery Chain）**:
从显式故障清除与急停复位开始，依次经过自检、契约与配置校验、新会话、重新规划和新授权执行包的单向恢复流程。
_Avoid_: 自动恢复、旧会话复活、旧 Bundle 续用

**剖面目标（Profile Target）**:
从获批联合执行剖面按规范采样得到、未经执行适配层修改的目标值。
_Avoid_: 实际下发值、实测状态

**应用目标（Applied Target）**:
执行适配层在允许的保守限幅后实际交给 CAN 层的目标值；与剖面目标的任何差异都必须显式报告。
_Avoid_: 规划目标、物理反馈、静默限幅值

**实测状态（Measured State）**:
由 STM32F4 和主机传感器观测到的实际运动与铺缆状态，不包含规划目标或应用目标。
_Avoid_: applied target、命令回显

**本地安全覆盖（Local Safety Override）**:
STM32F4 本地控制或安全逻辑对 NUC 应用目标的限制、替代或拒绝，必须作为执行反馈中的显式状态和原因报告；持续偏离联合执行包络会触发撤租。
_Avoid_: 不可见限幅、正常跟踪误差

**执行历元（Execution Epoch）**:
授权执行包在所属物理执行授权域本地安全时钟中固定的计划时间零点；执行适配层不得按接收、安装或首帧发送时间重置或平移。
_Avoid_: Adapter 本地起始时间、可滑动剖面时间轴

**v1 受信控制域（v1 Trusted Control Domain）**:
由主 NUC 的受控进程和物理隔离的双 NUC 点对点链路组成的 v1 信任边界。消息 hash 与 CRC 只提供完整性或误码检测；边界接入岸端、外部交换机、无线链路或非受信进程后必须重新安全评审并增加认证机制，否则不得武装。
_Avoid_: hash 即身份认证、CRC 即来源认证、把 v1 信任假设扩展到外部网络

**本地急停复位（Local Emergency Stop Reset）**:
默认只能通过受认证的本地操作入口发起的急停复位请求；v1 不定义远程复位、PKI 或密钥生命周期。
_Avoid_: 匿名复位、默认远程复位

**接口资源上限（Interface Limits）**:
约束消息大小、集合与字符串长度、待处理请求、地图分片以及日志和遥测队列容量的版本化配置，并作为契约清单的一部分。超限消息必须整体拒绝，不得截断或自动降采样后继续执行。
_Avoid_: 无界消息、超限截断、接口层轨迹降采样

**安全流量（Safety Traffic）**:
授权执行包、撤租、急停和执行反馈等不得被诊断爆量或地图分片堆积阻塞的高优先级通信流量。
_Avoid_: 与地图和诊断共享无优先级队列

**安全审计事件（Safety Audit Event）**:
使用事件、关联和因果身份连接授权、状态、故障、配置、会话和停车行为的结构化记录，使安全关键决策可确定性重建。审计写入不得阻塞安全动作，记录丢失必须显式可见。
_Avoid_: 仅自由文本日志、审计失败阻塞急停、静默丢日志
